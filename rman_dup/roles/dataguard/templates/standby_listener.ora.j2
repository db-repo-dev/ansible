LISTENER_RMAN =
    (DESCRIPTION =
        (ADDRESS = (PROTOCOL = TCP)(HOST = {{ target_host }})(PORT = {{ rman_port }}))
    )

SID_LIST_LISTENER_RMAN =
    (SID_LIST =
        (SID_DESC =
            (GLOBAL_DBNAME = {{ standby_sid }}_DGMGRL)
            (ORACLE_HOME = {{ oracle_home }})
            (SID_NAME = {{standby_sid}})
        )
    )
