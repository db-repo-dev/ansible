run{
  duplicate target database
  for standby
  from active database
  dorecover
  spfile
    set db_unique_name='{{ standby_sid }}'
    set audit_file_dest='/u01/app/oracle/admin/{{ standby_sid }}/adump'
    set control_files='+DATA_01/{{ standby_sid }}/CONTROLFILE/control01.ora','+GRID_INFRA/{{ standby_sid }}/CONTROLFILE/control02.ora'
    set local_listener='LISTENER_{{ standby_sid }}'
    set dispatchers='(PROTOCOL=TCP) (SERVICE={{ standby_sid }}XDB)'
  nofilenamecheck;
}  
